# Windsurf v3.19.5-windsurf.4 发布

## 发布背景

在 `release/windsurf-v3.19.4` 上发布供应商级 Auto 分类器跳过配置的增量版本。应用、Tauri crate、Cargo lock root 与 Tauri 配置版本仍统一为 `3.19.5`；本次使用递增预发布标签，不覆盖已有 `.1`、`.2`、`.3`。

## 本版本变更

- 当 Claude 供应商显式启用 `meta.skipAutoClassifier` 时，live settings 现在同时投影：
  - `permissions.defaultMode = "bypassPermissions"`
  - `sandbox.enabled = true`
  - `skipDangerousModePermissionPrompt = true`
- 直连、代理热切和代理备份均复用同一有效配置构建入口。
- 切走、关闭开关与通用配置同步会对称恢复供应商/共同片段原值，避免 live-only 注入固化到数据库或污染其他供应商。
- 四语言界面描述和 destructive confirmation 明确披露新增参数及危险模式确认被跳过的效果。
- 完整实现、调用链和风险记录见 [2026-09-06 跳过危险模式确认提示](./2026-09-06_跳过危险模式确认提示.md)。

## 提交范围

相对 `v3.19.5-windsurf.3`：

- `5df427be67d37b026262a518283f2578d9368fe5` — `feat(claude): skip dangerous mode prompt with classifier bypass`
- `91152d1c99b737c039f97f4eb716f9c3a6ac642f` — `style(claude): apply CI rustfmt output`
- 本发布归档提交。

## 验证

本地：

- `pnpm typecheck` — PASS。
- `pnpm format:check` — PASS。
- 修改测试 Prettier — PASS。
- `pnpm exec vitest run --dir tests --maxWorkers=2 --minWorkers=1` — 82 文件、518 用例 PASS。
- `git diff --check` — PASS。
- `pnpm build:renderer` 被当前执行策略拦截，未运行，不虚报结果。

远端：

- 首轮 [CI 34020630166](https://github.com/chunzhimoe/cc-switch/actions/runs/34020630166) 前端通过，三平台后端因相同的 3 处 rustfmt 排版差异失败。
- 第二轮 [CI 34036908672](https://github.com/chunzhimoe/cc-switch/actions/runs/34036908672) 全绿：Frontend Checks，以及 macOS、Windows、Linux 的 Rust fmt、Clippy、tests 全部 completed/success。

## 发布与风险

- 目标标签：`v3.19.5-windsurf.4`；`.github/workflows/release.yml` 将发布 prerelease 并执行 Windows x86_64/ARM64、Linux x86_64/ARM64、macOS universal 的完整 Tauri 构建。
- Fork 若未配置 Apple/Tauri 签名密钥，会按 workflow 降级生成 unsigned 安装包，不能当作官方签名/公证版本。
- 该功能是用户在供应商表单中显式确认开启的高风险选项；默认关闭，不会修改未启用供应商。
- 若 Claude Code 后续改变 `skipDangerousModePermissionPrompt` 的键名或读取时机，需要同步更新投影、恢复和文案。

## Git 状态

- 分支：`release/windsurf-v3.19.4`。
- 推送目标：用户 fork `https://github.com/chunzhimoe/cc-switch.git` 的同名分支；不改 remote、不推 main、不使用 force。
- 归档生成时 `.4` 标签尚未创建；应在发布归档提交的 CI 通过后创建并推送，再以 Release workflow 的实际结果更新状态。
